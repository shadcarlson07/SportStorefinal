@model SportsStore.Domain.Entities.Product

<div class="well">
    
    @if (Model.ImageData != null)
    {
        <div class="pull-left" style="margin-right: 10px">
            <img class="img-thumbnail" width="75" height="75" src="@Url.Action("GetImage", "Product", new { Model.ProductID })"/>
        </div>
    }

    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("c")</span>
    </h3>

  @*  <script>
        var quant = function () {
            var quanity = prompt("How many items would you like to add?");
            return quanity;
        }

        $(document).ready(function () {
            $('button').click(function () {
                $.ajax({""})
            })
        });

 </script> *@


    @using (Html.BeginForm("AddToCart", "Cart"))
    {
        <div class="pull-right">
             @Html.HiddenFor(x => x.ProductID)
             @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
             @Html.Hidden("quanityAdd", 1)
            <input type="submit" id="test" class="btn btn-success" value="Add to cart"/>
        </div>
    }

    <span class="lead"> @Model.Description</span>
</div>

